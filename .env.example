# financial_bot と同じキーが流用できる。.env にコピーして値を入れる。
# load_dotenv(override=True) で空のsystem env varsを上書きする（事故防止）。
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# 顔出しなしX自動投稿を使う場合のみ（X API v2）
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
